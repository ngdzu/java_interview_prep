# 004_01 — Stream Terminal Operations

This lesson focuses on Java Stream terminal operations: forEach, collect, reduce, min/max, findFirst/findAny, anyMatch/allMatch/noneMatch, count, summaryStatistics, joining, groupingBy, partitioningBy and toArray. 

Prerequisites
- Java 17
- Maven 3.6+

Build
```bash
mvn -q package
```

Run example
```bash
mvn -q exec:java -Dexec.mainClass="dev.playground.streamops.Main"
```

See `lesson.md` for the full lesson content and exercises.